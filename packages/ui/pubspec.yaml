name: prayer_times_ui
description: Shared design tokens, layout, and widgets.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  prayer_times_core:
    path: ../core

dev_dependencies:
  flutter_lints: ^5.0.0
