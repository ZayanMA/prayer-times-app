name: prayer_times_data
description: Drift database and repositories for the prayer times app.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  drift: ^2.24.0
  flutter:
    sdk: flutter
  http: ^1.3.0
  path: ^1.9.1
  prayer_times_core:
    path: ../core
  shared_preferences: ^2.5.2
  sqlite3: ^2.7.2

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.24.0
  lints: ^5.1.1
  test: ^1.25.14
